{
    "type": "csw-resource",
    "id": "{{resource.id}}",
    "name": "{{dataset.title or dataset.name}}",
    "url": "{{resource.url}}",
    "containerId": "{TARGET_METADATA_UUID}",
    "baseLink": "{{resource.url}}",
    "itemProperties": {
      "layers": "{{resource.name}}",
      "featureInfoTemplate": "",
      "customProperties": {
        "dimensionLabelsConfig": {
          "dimensionsJsonStatUrl": "",
          "showDimensionsOnPanel": false
        }
      },
      "ignoreUnknownTileErrors": true
    }
  }