{
    "name": "{{resource.name}}",
    "url": "{{resource.url}}",
    "description": "{{dataset.notes or resource.description}}",
    "id": "{{resource.id}}",
    "type": "{{resource.format|lower}}",
    "layers": "{{resource.name}}"
}